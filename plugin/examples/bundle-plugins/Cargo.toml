[package]
name = "bundle-plugins"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]

[dev-dependencies]
clack-host = { workspace = true, features = ["libloading"] }
